# Max 室内设计AI助手 — 配置指南

---

## 一、安装启动（2步）

### 1. 填写 .env（4个必填字段）

复制项目根目录的 `.env.example` 为 `.env`，填写：

```ini
LLM_API_KEY=你的DeepSeek_API_Key
FEISHU_APP_ID=cli_xxxxx
FEISHU_APP_SECRET=xxxxx
FEISHU_BITABLE_APP_TOKEN=xxxxx
```

**FEISHU_BITABLE_APP_TOKEN** 获取方法：打开飞书多维表格，URL 中 `/base/{App_Token}` 部分即是。

### 2. 初始化 + 启动

```bash
cd M:/ClaudeCode
python -m max_system init    # 首次运行：自动创建Bitable表 + 写入.env + 初始化数据库
python -m max_system         # 启动飞书模式（默认）
```

`max init` 做了三件事：
1. 检测 LLM 和飞书连接
2. 自动创建9张Bitable表 + 把table_id写回 `.env`
3. 初始化本地数据库（`data/max.db`）

---

## 二、飞书应用权限

在飞书开放平台 → 应用 → 权限管理中开通：

| 权限 | 用途 |
|------|------|
| `im:message` | 收发消息 |
| `im:message:send_as_bot` | 群聊中以机器人身份发言 |
| `bitable:app` | 读写多维表格 |
| `calendar:calendar` | 创建/查询/删除日历日程 |
| `approval:approval` | 创建审批（高风险操作需要） |
| `task:task` | 创建/查询/完成待办任务 |

---

## 三、目录结构

```
MAX/
├── .env                     ← 你填的配置文件
├── data/max.db              ← 本地数据库（自动创建）
├── knowledge/               ← 知识库（施工规范、案例、材料等）
│   ├── company_standards/
│   ├── case_database/
│   ├── material_database/
│   ├── customer_service/
│   └── media_materials/
├── quotes/                  ← 报价数据
│   ├── 材料库.json
│   ├── 施工库.json
│   └── 报价生成器.py
├── templates/               ← 文档模板（01~06期）
├── prompts/config/system_prompt.md  ← Max的提示词
└── max_system/              ← 系统代码（不要改动）
```

---

## 四、启动方式

```bash
python -m max_system              # 飞书长连接模式（默认，推荐）
python -m max_system cli          # CLI命令行模式（终端测试用）
```

**默认使用飞书长连接**，不需要公网IP、不需要内网穿透。

---

## 五、飞书多维表格（9张表）

由 `max init` 自动创建：

| 表名 | 用途 |
|------|------|
| 客户信息 | 客户全生命周期管理 |
| 合同管理 | 合同信息与付款进度 |
| 合作商 | 供应商/合作商管理 |
| 支出明细 | 项目支出记录 |
| 收入明细 | 项目收入记录 |
| 施工管理 | 施工节点与进度 |
| 任务 | 施工任务管理 |
| 售后维保台账 | 售后问题处理记录 |
| 跟进记录表 | 客户跟进与回访 |

---

## 六、首次对话引导

启动后，Max 会主动打招呼并询问：
- 公司叫什么名字？
- 主打什么设计风格？
- 服务哪些客户群体？在哪个城市？

设计师通过对话回答，Max 自动保存。之后可以说"把我们公司名改成XX"随时修改。

---

## 七、Max 能做什么

### 谈单
- 线索分类评级 → 需求分析 → 合同辅助 → 数据统计
- 查询材料库/施工库价格 → 报价汇总计算（管理费、税金、清运费、成品保护费 → 报价总计）
- 所有操作自动同步飞书表格

### 售后
- 竣工回访排程、问题处理工单、售后台账、老客户维护

### 自媒体
- 线索转交筛选、案例包装、数据复盘

### 飞书操作
- 发消息、读写表格、创建审批、日历日程
- 创建待办任务、查询任务列表
- 定时提醒（"下周三提醒我跟进王女士"）

---

## 八、常用命令参考

设计师在飞书中对 Max 说的话（示例）：

| 说的内容 | Max 做的事 |
|---------|-----------|
| "帮我分析张先生的需求" | 查客户信息 + 给出分析 |
| "把XX公司材料费下调5%" | 更新报价 |
| "下周提醒我签XX合同" | 创建飞书待办任务 |
| "统计本月签约数据" | 从表格拉数据统计 |
| "帮我算一下这个项目的总价" | 材料+施工明细 → 管理费、税金、汇总报价总计 |
| "管理费率调到10%" | 更新Profile中的费率配置 |
| "给XX客户发节日祝福" | 生成文案 + 发飞书消息 |
| "竣工3个月了回访一下XX" | 生成回访计划 + 写入跟进表 |
