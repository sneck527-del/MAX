# Max 多Agent系统 — 你需要提供的文件、链接 & 知识库标准

---

## 一、飞书开放平台配置 — 干什么用

需要注册一个**飞书企业自建应用**，获取以下信息：

| 配置项 | 当前值 | 在系统里干什么用 | 获取位置 |
|--------|--------|----------------|---------|
| **App ID** | `cli_a9504ca3a9389bd3` (已有) | 标识机器人身份，飞书API认证用 | 飞书开放平台 → 应用 → 凭证与基础信息 |
| **App Secret** | `UKS2E58S9KJ8Hm5yCO5JXgSKI8W2K5Qz` (已有) | App ID配套密钥，获取access_token用 | 飞书开放平台 → 应用 → 凭证与基础信息 |
| **Verification Token** | `JIyu7h6hfQjI4aVxm9alJ7YIQpOPGTrz` (已有) | 验证事件回调真实性，防止伪造请求 | 飞书开放平台 → 应用 → 事件与回调 |
| **Encrypt Key** | `95d99bb0e2862d8fc7b2e08ca4e07ca6` (已有) | 解密飞书推送的加密事件数据 | 飞书开放平台 → 应用 → 事件与回调 |
| **BitTable App Token** | **空 (需要你提供)** | **连接飞书多维表格，读写客户/合同/项目数据** | 线上表格URL中提取 |

**BitTable 获取方法：**
1. 打开线上飞书多维表格
2. URL 格式为：`https://sneck527.feishu.cn/base/{App_Token}?from=from_copylink`
3. 复制 `{App_Token}` 部分填入 `.env` 的 `FEISHU_BITABLE_APP_TOKEN`

### 飞书应用权限（在"权限管理"页面开通）

| 权限 | 在系统里干什么用 |
|------|----------------|
| `im:message` | Agent需要发消息回复设计师 |
| `im:message:send_as_bot` | 以机器人身份在群聊中发言 |
| `bitable:app` | Talker/Helper读写客户台账、项目进度表 |
| `approval:approval` | ApprovalGate创建审批流（高风险操作需设计师确认） |
| `calendar:calendar` | Helper创建量房/交底/验收日程事件 |
| `contact:contact` | 识别飞书消息是谁发的 |

### 多维表格字段结构

线上已有7张表 + 新建2张表，共9张表：

**线上已有表（直接使用）：**

| 表名 | Table ID | 在系统里干什么用 |
|------|----------|----------------|
| 客户信息 | `tbl6IdYFBB8RDFiO` | 客户全生命周期信息，Talker/Helper读写 |
| 合同管理 | `tblRS5zg0u5Hj6DN` | 合同信息与付款进度，Talker/Helper读写 |
| 合作商 | `tblnKdukg33OfUsN` | 供应商/合作商管理 |
| 支出明细 | `tbl2X6WH1RuCwBM3` | 项目支出记录（替代原项目台账支出侧） |
| 收入明细 | `tbl6WFZYHS19JHKk` | 项目收入记录（替代原项目台账收入侧） |
| 施工管理 | `tblLBj0GQik63K9W` | 施工节点与进度 |
| 任务 | `tblZA6hpoSVUbfTm` | 施工任务管理 |

**新建表（由 init_bitable.py 创建）：**

| 表名 | 在系统里干什么用 |
|------|----------------|
| 售后维保台账 | AfterPro售后问题处理与维保记录 |
| 跟进记录表 | 客户跟进与回访记录（回访+跟进合并） |

**客户信息表字段：**

| 字段名 | 类型 | 在系统里干什么用 |
|--------|------|----------------|
| 客户编号 | 文本 | 唯一标识（init_bitable.py 自动补建） |
| 客户姓名 | 文本 | |
| 联系方式 | 文本 | |
| 客户类型 | 单选 | |
| 客户来源 | 单选 | 小红书/抖音/朋友推荐/门店 |
| 项目地址 | 文本 | |
| 类型 | 单选 | 全案/半包/纯设计 |
| 户型 | 单选 | 大平层/别墅/复式等 |
| 面积 | 文本 | |
| 报价 | 数字 | |
| 设计费 | 数字 | |
| 分派设计师 | 多选 | |
| 录入时间 | 日期 | |
| 跟进时间 | 日期 | |
| 落单进度 | 单选 | 待评估/意向/谈判/签约/已签约 |
| 服务阶段 | 单选 | 新建/量房/方案/签约/施工/完工/归档 |
| 备注 | 文本 | |

---

## 二、需要你创建的目录 — 干什么用

### 2.1 Obsidian Vault 目录

| 目录路径 | 在系统里干什么用 | 操作 |
|---------|----------------|------|
| `M:/ObsidianVault/斑马精装/` | **Helper/AfterPro把项目文档、客户档案、售后记录归档到Obsidian** | **手动创建此文件夹** |

```bash
mkdir -p "M:/ObsidianVault/斑马精装"
```

如果已有Obsidian库，改 `.env` 里 `OBSIDIAN_VAULT_PATH` 指向你的Vault路径即可。

系统归档后的目录结构（自动生成）：
```
M:/ObsidianVault/斑马精装/       ← 这就是你的Obsidian Vault
├── 项目/                         ← 项目文档归档
│   └── {项目名称}/
│       ├── 归档索引.md           ← 自动生成的目录索引
│       ├── 量房单.md
│       ├── 需求分析表.md
│       ├── 设计方案说明.md
│       ├── 施工交底.md
│       └── 验收单.md
├── 客户/                         ← 客户档案归档
│   └── {客户姓名}/
│       ├── 客户信息.md
│       └── 跟进记录.md
└── 知识库/                       ← 知识库内容的Obsidian镜像
    └── ...
```

### 2.2 知识库目录

| 目录 | 在系统里干什么用 | 状态 |
|------|----------------|------|
| `06_知识库/company_standards/` | Talker/AfterPro查施工标准、合规检查 | ✅ 已创建 |
| `06_知识库/case_database/` | Talker谈单展示案例、MediaPro包装素材 | ✅ 已创建 |
| `06_知识库/material_database/` | 生成报价、选材参考 | ✅ 已创建 |
| `06_知识库/customer_service/` | AfterPro售后回复话术、Helper文档模板 | ✅ 已创建 |
| `06_知识库/media_materials/` | MediaPro生成自媒体内容参考 | ✅ 已创建 |

### 2.3 其他目录

| 目录 | 在系统里干什么用 | 状态 |
|------|----------------|------|
| `01_Max总控/log_audit/` | 存放操作审计日志数据库 | ⚡ 系统自动创建 |
| `Quote/` | 材料库/施工库JSON数据，报价工具读取 | ✅ 已有Quote项目 |

---

## 三、知识库文件 — 干什么用 & 标准

所有知识库文件统一使用 **Markdown (.md) 格式**，每个文件被 `knowledge_search` 工具全文检索。

### 通用规范

| 项目 | 要求 |
|------|------|
| 编码 | UTF-8 |
| 格式 | Markdown |
| 标题 | `# 一级标题` 开头，层级清晰 |
| 文件名 | 中文命名，见名知义，如 `施工规范.md` |
| 换行 | 统一用 LF（`\n`） |

### 3.1 公司标准规范 (`company_standards/`)

**干什么用：** Talker谈单时查设计尺寸 / AfterPro处理客诉时查施工标准 / Helper生成文档时套模板。
**知识库搜索关键词示例：** 防水高度、强弱电间距、闭水试验、墙面垂直度

```
# {规范名称}

## {第一类标准}
1. 具体条目 — 必须包含数据/尺寸/参数
2. 具体条目

## {第二类标准}
1. 具体条目
2. 具体条目

## 注意事项
- 常见违规点
- 红线要求
```

**建议新增文件：**

| 文件名 | 干什么用 | 包含内容 |
|--------|---------|---------|
| `施工规范.md` | ✅ 已创建 | 水电/防水/泥工标准 |
| `设计规范.md` | ✅ 已创建 | 常用尺寸/布局原则 |
| `验收标准.md` | 设计师验收时对照 | 各工种验收标准和允许误差 |
| `材料环保标准.md` | 回答客户环保疑问 | 环保等级/E0标准/检测报告 |
| `安全施工规范.md` | 合规检查红线 | 结构安全/消防/严禁事项 |

### 3.2 案例库 (`case_database/`)

**干什么用：** Talker谈单时给客户看同户型案例 / MediaPro包装成自媒体素材 / 设计师参考设计手法。
**知识库搜索关键词示例：** 别墅、大平层、现代简约、LDK、收纳

```
# {项目名称} - {风格}实景案例

## 项目信息
- 户型：{面积}㎡ {户型类型}
- 风格：{风格}
- 预算：{预算}
- 工期：{工期}

## 设计亮点
1. 亮点一
2. 亮点二

## 材料清单
- 地砖：{品牌/型号/规格}
- 墙面：{材质/颜色}

## 客户反馈
"{客户原话}"
```

**建议新增：** 每完工一个项目，加一个 `.md` 文件。

### 3.3 材料库 (`material_database/`)

**干什么用：** Helper生成报价时查材料价格 / Talker回答客户材料咨询。
**知识库搜索关键词示例：** 瓷砖、地板、马桶、橱柜、多乐士

```
# {材料类别}

## {品牌/品类}
- {产品}：{规格} {参考价} — {备注}
```

**建议新增：**

| 文件名 | 干什么用 | 包含内容 |
|--------|---------|---------|
| `供应商名录.md` | 采购联系 | 供应商/联系方式/合作条件 |
| `辅材清单.md` | 报价辅材项 | 水泥/沙子/腻子品牌规格 |
| `主材价格表.md` | 快速报价 | 常用主材品牌和价格区间 |

### 3.4 客服话术 (`customer_service/`)

**干什么用：** Talker和AfterPro回复客户时调用标准话术，保持品牌统一。
**知识库搜索关键词示例：** 报价咨询、投诉、量房、转介绍

```
## {场景名称}
{话术内容}

## {另一场景}
{话术内容}
```

**建议新增：**

| 文件名 | 干什么用 | 包含内容 |
|--------|---------|---------|
| `常见问题FAQ.md` | 快速回复常见问题 | 面积怎么算/工期多久/怎么付款 |
| `异议处理话术.md` | 应对客户异议 | 价格贵/质量差/工期慢的回应 |

### 3.5 自媒体素材 (`media_materials/`)

**干什么用：** MediaPro生成小红书/抖音文案时参考风格调性和选题。
**知识库搜索关键词示例：** 小红书、抖音、选题、爆文

```
# {平台/主题}

## 账号定位
{一句话定位}

## 内容方向
1. 方向一

## 运营动作
- 动作一
```

**建议新增：**

| 文件名 | 干什么用 |
|--------|---------|
| `抖音运营指南.md` | 抖音视频脚本模板、拍摄建议 |
| `选题库.md` | 各平台选题灵感 |
| `爆文标题模板.md` | 小红书标题公式 |

### 编写注意事项

1. **用实际数据** — 尺寸、价格、参数务必真实，LLM会直接引用
2. **可搜索性** — 多用业务中会提到的关键词（如"防水""收纳""LDK"）
3. **纯Markdown即可** — 不需要图片/表格样式，系统全文检索
4. **持续补充** — 新规范、新材料、新案例随时加文件
5. **文件名唯一** — 不同目录下文件名不要重复

---

## 四、API 密钥与模型 — 干什么用

| 项目 | 当前值 | 在系统里干什么用 | 说明 |
|------|--------|----------------|------|
| **DeepSeek API Key** | `sk-5bac0bede8fc4f73a3fd28cc3103c157` (已有) | **驱动所有Agent的对话/推理/函数调用**（默认模型） | 已测试可用 |
| DeepSeek 模型 | `deepseek-chat` | Max/Agent/LLM默认模型 | 可用，可换`deepseek-reasoner` |
| Ollama 模型 | `qwen3.5` | 本地备选模型（断网或省钱时用） | 需先 `ollama pull qwen3.5` |
| LLM 提供者 | `deepseek` | 控制使用云端还是本地 | 改 `.env` 中 `LLM_PROVIDER=ollama` 切换 |

**无需提供任何新密钥。** DeepSeek Key 已经配置且在 CLI 模式测试通过。

---

## 五、内网穿透 — 干什么用

| 工具 | 链接 | 什么时候需要 |
|------|------|-------------|
| **ngrok** | [https://ngrok.com/download](https://ngrok.com/download) | 用Webhook模式替代长连接时 |
| **frp** | [https://github.com/fatedier/frp](https://github.com/fatedier/frp) | 用Webhook模式且有自建服务器时 |

**默认使用长连接模式（`python -m max_system feishu`）**，无需内网穿透。

---

## 六、完整启动检查清单

```
 类别           项目                                   干什么用                                       状态
──── ────────────────────────────────── ───────────────────────────────────────── ─────────────────────
 飞书  □ BitTable App Token 已填入      Agent读写客户/合同/项目数据                      ❌ 缺你提供
	 飞书  □ 运行 init_bitable.py            新建售后维保+跟进记录表，补建客户编号字段          ❌ 需运行
	 飞书  □ 新建表ID已填入.env              售后维保台账/跟进记录表的table_id                 ❌ 需填入
 飞书  □ 应用已发布/启用                  机器人上线                                     ❌ 需操作
 飞书  □ 机器人已添加到群聊                群聊中@机器人触发                              ❌ 需操作
 飞书  □ 应用权限已全部开通                发消息/读写表格/审批流/日历                     ❌ 需操作
 目录  □ Obsidian Vault已创建            项目文档/客户档案归档                           ❌ 需创建
 API   □ DeepSeek API Key 有效           所有Agent的AI能力                              ✅ 已通过
 依赖  □ 依赖已安装                       Python包                                      ✅ 已完成
 知识  □ 知识库内容已确认                  施工规范/案例/材料/话术/自媒体素材               ✅ 有示例
 报价  □ 报价库文件已确认                  Quote/材料库.json                             ✅ 已有
```

---

## 七、启动命令

```bash
cd M:/ClaudeCode

python -m max_system feishu     # 飞书长连接模式（默认，推荐）— 通过飞书聊天对话
python -m max_system cli        # CLI模式 — 终端直接对话，适合测试
python -m max_system webhook    # Webhook模式 — 需要内网穿透
```

---

> **最核心：飞书多维表格 App Token。** 拿到后填进 `.env` 的 `FEISHU_BITABLE_APP_TOKEN`，运行 `python -m scripts.init_bitable` 新建售后维保台账和跟进记录表，将输出的 table_id 填入 `.env`，再创建好 Obsidian Vault 目录，发版飞书应用并添加到群聊，就可以启动了。
